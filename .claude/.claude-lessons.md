# Claude Lessons Log — Career Twin

---

## Deployment — Current hosting (as of 2026-05)
- **Frontend:** Netlify
- **Backend:** Render — `https://career-twin.onrender.com`
- Railway was abandoned due to repeated build failures.
- Vercel was abandoned in favour of Netlify.

---

## Lesson 14 — Pin Python version with .python-version when using Railpack
**Session:** 2026-04-09 A
Railpack auto-selects the latest Python (3.13 as of Apr 2026). Many packages (pydantic-core, PyMuPDF, older torch) have no pre-built wheels for 3.13 and fail to compile from source (needs Rust, MuPDF, etc.).
**Fix:** Add `.python-version` containing `3.11` at the Railway root directory. Railpack (via mise) reads it and installs that version.
**Apply when:** Any Railway Railpack Python deploy — always pin the version.

---

## Lesson 13 — Railpack only copies requirements.txt — no -r includes work
**Session:** 2026-04-09 A
If `requirements.txt` uses `-r requirements-base.txt`, Railpack only uploads `requirements.txt` to the build container. The referenced file isn't there and pip fails with "No such file or directory".
**Fix:** Make `requirements.txt` fully self-contained with no `-r` references for Railway. Use separate files only for local/Docker installs.
**Apply when:** Any Railpack deployment with split requirements files.

---

## Lesson 12 — CPU-only torch for Railway — GPU wheel is 530MB and times out
**Session:** 2026-04-09 A
`sentence-transformers` pulls in full GPU torch (~530MB) by default. Railway's build network connection resets mid-download.
**Fix:** Add `--extra-index-url https://download.pytorch.org/whl/cpu` and pin `torch==2.5.1+cpu` in the Railway requirements. Verify available versions from the error log — not all versions exist in the CPU index.
**Apply when:** Any Railway/cloud deployment using sentence-transformers or torch.

---

## Lesson 11 — Nixpacks won't find Python if requirements.txt is not at repo root
**Session:** 2026-04-09 A
Nixpacks auto-detects Python by looking for `requirements.txt` at the repo root. If it's in a subdirectory (e.g. `career-twin/backend/requirements.txt`), Nixpacks uses the bare Ubuntu image with no Python/pip, causing `pip: command not found` (exit code 127).
**Fix:** Add `providers = ["python"]` at the top of `nixpacks.toml` to force the Python provider regardless of file location.
**Apply when:** Any Railway/Nixpacks deploy where requirements.txt is not at the repo root.

---

## Lesson 10 — Railway switched default builder to Railpack — nixpacks.toml is ignored
**Session:** 2026-04-09 A
Railway's default builder is now Railpack, which does not read `nixpacks.toml`. Deploying without a `railway.toml` causes `Error creating build plan with Railpack`.
**Fix:** Add a `railway.toml` at the repo root with `[build] builder = "NIXPACKS"` to force Nixpacks. Also set `[deploy] startCommand` there as the canonical start command.
**Apply when:** Any Railway deployment that relies on `nixpacks.toml`.

---

## Lesson 9 — TextShimmer via `motion.create()` unreliable in Tailwind v4
**Session:** 2026-04-08 C
Using `motion.create(Component as any)` with `backgroundPosition` animation and Tailwind arbitrary CSS variable classes (`[--base-color:...]`) does not render reliably in Next.js + Tailwind v4. The shimmer gradient never appeared.
**Fix:** Bypass the abstraction entirely — use a plain `motion.h1` with inline `style={{ backgroundImage: "...", backgroundSize: "200% 100%", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent" }}` and animate `backgroundPosition` directly. No component wrapper needed.
**Apply when:** Any text shimmer effect in this project. Skip TextShimmer component for actual usage.

---

## Lesson 8 — On-demand LLM popup: open modal immediately, load detail async
**Session:** 2026-04-08 B
For features that expand a list item into rich detail (project cards, role cards), open the modal immediately with a skeleton state, then fire the API call. Modal state (open/detail/error) lives in the parent that owns the list. The retry handler needs to re-derive `shortDesc` from the original data list since the modal only stores the project name.
**Apply when:** Any card or list item that expands into a detail view via an API call.

---

## Lesson 7 — Wrap `json.loads` and Pydantic construction in try/except in every LLM service
**Session:** 2026-04-08 B
All LLM services had bare `json.loads(content)` and bare `Model(**data)` calls. A malformed LLM response raises `JSONDecodeError` or `ValidationError`. Fix: wrap both in try/except and raise `ValueError` with a human-readable message.
**Apply when:** Creating any new service that parses LLM JSON output.

---

## Lesson 6 — Subagent tool permissions can silently fail — fall back to inline edits
**Session:** 2026-04-08 A
Haiku subagents dispatched for simple file creation hit permission restrictions and returned BLOCKED. Fix: implement the task directly in the controller session using Write/Edit/Bash.
**Apply when:** Any subagent returns BLOCKED due to tool permissions on trivial file tasks.

---

## Lesson 5 — `skills` field on `RoleSuggestion` must be declared early
**Session:** 2026-04-08 A
The roles page tried to display role skills before analysis completed. `RoleSuggestion` had no `skills` field so the type check broke.
**Fix:** Add `skills: list[str] = []` to both the Pydantic model and the TypeScript type simultaneously when a new field is needed on a shared contract object.
**Apply when:** Adding any new field to a shared backend/frontend type — update both sides atomically.

---

## Lesson 4 — ProfileForm needs section nav with IntersectionObserver
**Session:** 2026-04-08 A
A long single-scroll form with 4+ sections needs a sticky left nav with IntersectionObserver to track active section. Active section highlight uses `scrollIntoView({ behavior: 'smooth' })` on click.
**Apply when:** Any form or content page with 4+ logical sections.

---

## Lesson 3 — Parallel background analysis with per-card loading state
**Session:** 2026-04-08 A
Waiting for 3 role analyses sequentially felt slow. Fix: kick off `analyzeRoleFit` for all roles in parallel on page load, track a `Set<string>` of `loadingIds`, update each card independently. Cache results in `sessionStorage` keyed by role title.
**Apply when:** Any page that fires multiple independent API calls that could arrive out of order.

---

## Lesson 2 — Structured `priority_improvements` beats flat string list
**Session:** 2026-04-08 A
`list[str]` for improvements gave the frontend no structure for cards/CTAs. Switching to `list[{area, title, detail, action}]` lets UI render area badge, headline, explanation, and action button from one field.
**Apply when:** Any list the frontend needs to render as cards or stepped UI.

---

## Lesson 1 — Scoring calibration must be explicit in the prompt
**Session:** 2026-04-08 A
LLM inflated match scores by default. Fixing tone alone wasn't enough. Fix: embed explicit score tables with exact criteria per tier (e.g., "70-84: Has 70-89% of required skills").
**Apply when:** Any prompt that produces a numeric score the user relies on.

---

## Lesson — Preview_match_score needs Pydantic range validation
**Session:** 2026-04-07 B
A plain `int` field with a comment `# 0–100` is not enforced. Pydantic will accept 150 or -5.
**Fix:** Use `Field(..., ge=0, le=100)` whenever a numeric field has a bounded range.

---

## Lesson — LLMs often wrap JSON in markdown fences — strip before json.loads
**Session:** 2026-04-07 B
Groq returns ` ```json\n{...}\n``` ` even when instructed not to. `json.loads()` throws `JSONDecodeError`.
**Fix:** Always strip fences: `if content.startswith("```"): content = content.split("\n",1)[-1]; content = content.rsplit("```",1)[0].strip()`. Apply to every LLM service.

---

## Lesson — "Load failed" on Safari = fetch() network error, not an HTTP error
**Session:** 2026-04-07 B
Safari reports `TypeError: Load failed` when `fetch()` fails at the network level. NOT caught by `if (!res.ok)`.
**Fix:** Wrap `fetch()` itself in try/catch and show a human-readable message.

---

## Lesson — Subagent worktrees vs main branch
**Session:** 2026-04-07 B
With `isolation: "worktree"`, commits land on a separate branch and must be merged back. For sequential tasks that build on each other, run without isolation to avoid merge overhead.

---

## Lesson — Shell state does not persist between Bash tool calls
**Session:** 2026-04-07 A
Each Bash call is a new shell. `source .venv/bin/activate` has no effect on the next call.
**Fix:** Always use full venv binary paths: `.venv/bin/pytest`, `.venv/bin/pip`, etc.

---

## Lesson — Pre-creating directories breaks create-next-app
**Session:** 2026-04-07 A
Running `mkdir -p frontend/{app,components,...}` before `create-next-app` causes "directory contains conflicting files".
**Fix:** Let `create-next-app` scaffold the frontend directory itself.

---

## Lesson — python-multipart must be in requirements.txt from day one
**Session:** 2026-04-07 A
FastAPI `UploadFile` requires `python-multipart` or file upload endpoints silently fail.

---

## Lesson — Always implement mock fallback for LLM services
**Session:** 2026-04-07 A
Without `GROQ_API_KEY` the route would 500 and block all frontend dev.
**Fix:** Implement `_mock_structure()` that returns plausible demo data when API key is empty.

---

## Lesson — Tailwind v4 + shadcn/ui CLI may fail — have a manual fallback ready
**Session:** 2026-04-07 A
`npx shadcn@latest init` can fail due to network errors or Tailwind v4 incompatibility.
**Fix:** Manually create `components.json`, add CSS variables to `globals.css`, copy component files directly.

---

## Lesson — Dashboard polish lands faster when color roles are centralized first
**Session:** 2026-04-08 B
Trying to restyle each dashboard component ad hoc creates inconsistencies across sidebars, chips, and modals.
**Fix:** establish shared surface, border, accent, and status tones in `app/globals.css`, then update component-level classes against that system.
